```
syscall-lab/
├── 00-c-refresher/        # calculadora, ponteiros, malloc/free, structs
├── 01-linux-env/          # compilação, argv, pipes, tempo (Semana 3)
├── 02-processes/          # fork, exec, wait, zumbis/órfãos
├── 03-ipc/                # race conditions, busy wait, semáforos, prod/cons
├── 04-scheduling/         # FCFS, SJF, RR, preemptivo (simuladores)
├── 05-threads/            # pthreads, mutex, condition variables
├── 06-memory/             # alocadores, paginação, memória virtual, mmap
├── 07-filesystem/         # open/read/write, inodes, diretórios, FS simulado
├── 08-io/                 # buffering, dispositivos, camadas de E/S
├── docs/                  # anotações do Tanenbaum + resumos por TE
├── Makefile               # alvos por pasta, -Wall -Wextra -g -fsanitize
└── README.md
```
